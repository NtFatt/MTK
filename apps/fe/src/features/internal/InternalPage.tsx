/**
 * Internal area — page stub for /internal (PR-00).
 * Full internal routes in later PRs.
 */
export function InternalPage() {
  return (
    <main className="p-6">
      <h1 className="text-xl font-semibold text-foreground">Internal</h1>
      <p className="mt-2 text-muted-foreground">Placeholder. Internal layout and routes later.</p>
    </main>
  );
}
